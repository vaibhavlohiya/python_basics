"""
A python program on how to use modules.
"""
# We have already created a file that we will import here to use it's functions and so on.

import tools_for_modules # this is to import the file that we have already created.

print(tools_for_modules.roll_dice(10))