"""
A python program about tuples.
"""

coordinates = (4, -7)  # tuples are immutable (you cannot change them after creation)

print(coordinates[0])

coordinates = [(4, -7), (0, 5), (8,-1)] # list of tuples